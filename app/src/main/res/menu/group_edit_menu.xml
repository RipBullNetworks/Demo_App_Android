<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

  <item
      android:id="@+id/action_edit"
      android:icon="@drawable/ic_edit_profile_picture"
      android:title="@string/menu_attachment"
      android:visible="false"
      app:contentDescription="@string/menu_attachment"
      app:showAsAction="ifRoom"/>

  <!--android:icon="@drawable/ic_account_multiple_plus_outline"-->
  <item
      android:id="@+id/action_add_participants"
      android:title="@string/menu_add"
      android:visible="false"
      app:contentDescription="@string/menu_add"
      app:showAsAction="ifRoom"/>

</menu>
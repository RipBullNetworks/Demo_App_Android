<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"   >

  <solid android:color="@color/thread_count_box_bg" />

  <stroke
      android:width="1dp"
      android:color="@color/thread_stroke_color" />

  <corners
      android:topLeftRadius="@dimen/thread_count_box_corner"
      android:topRightRadius="@dimen/thread_count_box_corner"
      android:bottomRightRadius="@dimen/thread_count_box_corner"
      android:bottomLeftRadius="@dimen/thread_count_box_corner"/>

</shape>